# GitHub Packages registry for @cyberark scope
@cyberark:registry=https://npm.pkg.github.com

# Authentication token (use environment variable)
# Set GITHUB_TOKEN in your environment or CI/CD
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Optional: Auto-install peer dependencies
auto-install-peers=true
strict-peer-dependencies=false
